<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Momence Schedule</title>
</head>
<body>

    <!-- Ribbon Schedule Container -->
    <div id="ribbon-schedule"></div>

    <!-- Momence Schedule Plugin -->
    <script
        async
        type="module"
        host_id="13752"
        teacher_ids="[]"
        location_ids="[]"
        tag_ids="[87689]"
        session_type="class"
        show_additional_teachers="true"
        hide_tags="true"
        default_filter="show-all"
        src="https://momence.com/plugin/host-schedule/host-schedule.js"
    ></script>

    <!-- Fallback for script not working -->
    <noscript>
        <p>The schedule cannot be displayed because JavaScript is disabled or the script failed to load.</p>
    </noscript>

    <!-- Add console logging for debugging -->
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            console.log("Momence Schedule Plugin Loaded");
            console.log("Host ID: 13752");
            console.log("Teacher IDs: []");
            console.log("Location IDs: []");
            console.log("Tag IDs: [87689]");
        });

        // Check for errors during script load
        window.addEventListener('error', function (e) {
            console.error('Error loading schedule:', e.message);
        });
    </script>
</body>
</html>
